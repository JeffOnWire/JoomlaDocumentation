CM7.6: markup/grammar fixes after Jeff's major expansion with
eighteen screenshots

Jeff substantially expanded this file: eighteen real screenshots,
a much fuller "Permissions for Individual Articles" section that
now also covers the Public role (not just Author/Editor/Publisher),
and real UI details from testing (the simplified frontend
new-article form, fewer toolbar tools for non-Publishers, the
Content tab's expandable toolbar for Publishers). All genuine
content improvements, kept as-is.

Fixed a batch of mechanical issues found in review:

- Same <strong>-wrapping-an-<img> bug as CM7.2 (list_of_new_users.png
  pasted inside the Save & Close tags) - moved outside.
- Malformed <em>/<br> nesting around the front-end New Article
  figure, likely a WYSIWYG paste artifact - cleaned up.
- One real dimension mismatch (newsroom_subscriptions_first_look.png,
  labelled 1154x434, actually 1156x436) - fixed.
- Missing trailing <br><br> after two screenshots, a quadruple
  <br><br><br><br> after another - both fixed to match the file's
  own spacing convention.
- "Click on the X" appeared twice (ellipsis icon, Content tab) -
  the only instances in the file, inconsistent with "Click X" used
  everywhere else in the project. Fixed both.
- "Click View the Site" -> "Click View Site" to match the actual
  button label used elsewhere.
- Real typos: "in then frontend" -> "in the frontend"; "not allow
  to edit" -> "not allowed to edit"; "Result." (unbolded period,
  no colon) -> "Result:" matching every other instance.
- Stray spaces inside <em> tags around an article title - moved
  outside.
- Permission action names (Delete, Edit, Edit State) weren't
  italicized like the rest of the section's on-screen labels
  (Inherited, Create, Edit Own) - fixed for consistency.
- Two "Save & Close." with the period inside the <strong> tag,
  inconsistent with the rest of the file - fixed.
- Style: two "enter a few words into the body, [example]" comma
  constructions changed to colons for readability.

Verified: all eighteen screenshot dimensions match their real
files, tags balanced, zero non-breaking spaces, zero "Click on"
instances remaining.
