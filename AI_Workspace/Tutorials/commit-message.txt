Section 2 (CM2.1-2.5): add real screenshots, fix text/screenshot
mismatches found along the way

Placed all 24 screenshots Jeff shot on the live site across the five
files (3/4/7/6/4 respectively), verifying each against the drafted
text before placing it, same process used for every prior section.

Corrections found and applied:

- CM2.3: confirmed "Choose a Category" is the real field label, and
  that clicking the category name in the breadcrumb (e.g. "Company
  Articles Blog") really does navigate back from a subcategory page -
  both were open items, now resolved.
- CM2.4: reworded "Observe that the Home menu item is..." to "Look at
  the Home row... the list already shows..." - the menu type is
  visible directly in the list without opening the item, which the
  original phrasing didn't make clear. Added the "Article featured."
  confirmation message to the list-icon-click step's result.
- CM2.5: added Category: Company to the Careers article (Jeff's
  screenshot showed it assigned there; the draft text hadn't set a
  category, which would have made the screenshot and text disagree).
  Changed "Select Article: the link to the article" and "Menu: the
  default menu, most likely Main Menu" to their literal confirmed
  values, "Careers" and "Main Menu".

Noted one screenshot artifact that didn't need a text fix: CM2.1's
new-menu-item screenshot shows "Household Pets" in the Ordering
dropdown, a leftover from the earlier pet-themed test site build.
Doesn't affect anything the tutorial text claims.

Section 2 is now complete end to end with real, verified screenshots
throughout - no open items remain.
