CM7.5: fix Result-split violation, a dimension bug, and a typo
after Jeff's screenshots

Jeff's edits replaced "close the browser tab or window" with "click
the browser back button to return to the list" (both places) - a
real refinement from testing, kept as-is.

Fixed: the back-button action and its "Result: the article appears
with a lock symbol" had been split into two separate <li> elements
instead of staying together - same class of Style Guide violation
caught in CM7.4 last round, merged back into one.

locked_icon_hover.png's height attribute said 377px; the real file
is 303px. Fixed to match.

Typo in a figcaption: "an articled checked-out by someone else" ->
"an article checked out by someone else."

Lowercase Result text ("a check-in message appears") capitalized,
plus a missing trailing <br><br> added after that screenshot to
match the file's other plain-img screenshot spacing.

Flagged the img/figure mix, then resolved as intentional: Jeff
explained it traces back to Joomla's own image-insert dialog (the
caption field alone decides which tag you get), not a deliberate
per-image choice - fixing it means editing raw HTML, trivial for me
but a real barrier for other editors. Decided to leave this
inconsistency alone project-wide and added it as an explicit rule
to the Tutorial Style Guide so it stops getting flagged.

Verified: all three screenshot dimensions now match their real
files, tags balanced, no stray characters.
