Section 2: apply post-redesign rules, add CM2.5 nav, new screenshot spacing

CM2.1-2.5's fixture redesign finished before the log-in-once and
Click-X-first-time rules existed. Re-reviewed against the full
current style guide and fixed what those rules missed:

- CM2.2: "Save & Close" (x4) and "Save & New" (x2) were all still
  "Click"-prefixed; fixed so only the first of each is "Click X,"
  every repeat is bare. "Click New again" (second sub-category)
  changed to bare "New." to match the project's fragment-style
  repeat convention.
- CM2.3: real bug - Setup's own actions (create two articles) were
  written before the login step, which was misplaced in Steps
  after Setup instead of being Setup's first step. Fixed. Also
  fixed a repeated "Click Save & Close" (x2) and a repeated
  "Click New" for the second menu item form, both now bare.
- CM2.5: real bug - no login step existed anywhere in the file.
  Added as the first step of "Save an Article to a Menu." Also
  fixed a repeated "Click Save & Close" that should have been bare.

CM2.5 follow-up: added the explicit "Content > Articles" navigation
and "Click New" step that the article half was missing (the
category half already had both) - confirmed as a real gap, not
intentional brevity.

New screenshot spacing rule, piloted on Section 2 only for now:
two <br> tags always precede an image; a plain <img> (no caption)
gets two more <br> after it; a <figure>/<figcaption> gets none
after. Style Guide updated. Applied to all 24 figures in
CM2.1-2.5 (all figure/figcaption in this section - every one now
double-<br>-before, none-after).

Verified: tag balance, image dimensions against real files, and
non-breaking spaces all clean across all five files.

Not yet done: the new spacing rule hasn't been applied to any
other section yet (including CM7's undocumented double-<br>
convention, which predates this and doesn't match it either -
CM7 has no trailing <br> removed). Pending Jeff's confirmation
before rolling out further.
