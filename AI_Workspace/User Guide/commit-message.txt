User Guide: create Local Setup overview page (410)

- Add overview-pages/410_Local_Setup.html: new article, replacing the
  category-list menu item that currently serves as the overview for
  Local Setup (guide.joomla.org/user-manual/local-setup), whose intro
  text lived in the menu item's description field rather than an
  article. Intro plus flat list of its six articles (Local Hosting on
  Windows, Laragon Setup (Windows), Local Hosting on Linux, Docker
  Setup, Local Test Setup, Raspberry Pi Installation). Borderline-sized
  section (six articles) with only one weak natural subgroup (the two
  Windows articles), so kept flat per the style guide rather than force
  a subsection split.

- Nav label vs. article H1 mismatch worth noting: the nav item "Local
  Test Setup" points to an article whose own H1 is "Install Joomla with
  Tests" - used the nav label per the style guide.

No Sync-from-Joomla baseline file existed for 410 - drafted directly
from the live site's current content and menu.

Flag: the existing category-list description had a curated list of
AMP-stack / local-hosting tool recommendations with links, plus a
deprecation note and a third-party script mention, none of which are
covered by any of the six articles. The general "what you need" framing
is folded into the new intro, but the specific tool list has no other
home - see Claude's chat message and the GitHub issue draft for exact
wording and a suggested destination.
