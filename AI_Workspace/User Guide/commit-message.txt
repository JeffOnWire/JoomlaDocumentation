User Guide: create Schema.org overview page (409)

- Add overview-pages/409_Schema.org.html: new section overview for
  Schema.org (guide.joomla.org/user-manual/schema-org) - intro plus two
  subsections (Getting Started with Schema.org; Article Schema Types)
  covering all nine articles. Nine articles split naturally into a
  3-article setup group and a 6-article "one per schema type" group, so
  subsections used per the style guide.

No Sync-from-Joomla baseline exists for 409 because there was no prior
overview page to sync - unlike every other section so far, the
guide.joomla.org/user-manual/schema-org URL currently has no overview
article at all; it lands directly on "An Introduction to Schema.org"
(the first article in the section). This is a genuinely new article,
not a rewrite.

Implementation note for Jeff: once 409 is created and published in
Joomla, the "Schema.org" menu item / section link will need to be
repointed from An Introduction to Schema.org to the new overview
article, the way every other section's heading link points at its
overview page.
